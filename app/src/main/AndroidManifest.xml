<?xml version='1.0' encoding='utf-8'?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.debashis.io.material.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
       android:largeHeap="true"
		android:name=".Applications"
        android:resizeableActivity="false">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
				<action android:name="android.intent.action.MAIN"/>
				
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
	android:name=".DebugActivity"
	android:screenOrientation="portrait"/>
        
      <meta-data
          android:name="android.max_aspect"
          android:value="4.0"/>

        <activity
            android:name=".TabLayoutActivity"
			android:theme="@style/AppThemes"
            android:label="@string/tab_layout"/>
        <activity
            android:name=".ScrollableTabActivity"
			android:theme="@style/AppThemes"
            android:label="@string/scrollable_tab"/>
        <activity
            android:name=".BadgeTabActivity"
            android:theme="@style/AppThemes"
            android:label="@string/tab_with_badge_view"/>
        <activity
            android:name=".FixedTabActivity"
            android:label="@string/app_name"/>
        <activity
            android:name=".DynamicTabActivity"
            android:label="@string/app_name"/>
    </application>
	
</manifest>
